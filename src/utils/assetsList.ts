const imageFolderName = 'assets/images';
const flagFolderName = '';

const flagArray = [
	// 'aus',
];

export const loadAssetsFirst: { [key: string]: string } = {};
export const loadAssetsLater: { [key: string]: string } = {};
export const textureList: string[] = [];

export const dataJsonUrl = './assets/data.json';

// =============================================
